export const checkValue = (newValue, oldValue) => {
  if (newValue === oldValue) {
    return oldValue;
  } else {
    return newValue;
  }

  // if (newValue !== undefined) {
  //   return newValue
  // } else {
  //   return oldValue
  // }
}